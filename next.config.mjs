/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images:{
    domains: ['lecture_1.vercel.app'],
    domains: ['inflearn-nextjs.vercel.app'],
  },
};

export default nextConfig;
